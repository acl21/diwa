defaults:
  - _self_
  - env@env_cfg: calvin
hydra:
  run:
    dir: ${logdir}
_target_: diwa.agent.eval.eval_diffusion_agent.EvalDiffusionAgent

name: ${env_name}_eval_diffusion_mlp_ta${horizon_steps}_td${denoising_steps}
logdir: ${oc.env:DIWA_LOG_DIR}/calvin-eval-feat/${name}/${now:%Y-%m-%d}_${now:%H-%M-%S}_${seed}
base_policy_path: ${oc.env:DIWA_LOG_DIR}/calvin-pretrain-feat/${env_name}_pre_diffusion_mlp_feat_ta4_td20/2025-07-31_16-28-30_42/checkpoint/state_5000.pt
normalization_path: null

seed: 42
device: cuda:0
env_name: close_drawer
env_type: calvin
obs_dim: 2048
action_dim: 7
denoising_steps: 20
ft_denoising_steps: 0 # Use 0 for base policy evaluation
cond_steps: 1
horizon_steps: 4
act_steps: ${horizon_steps}

comment: ""
offline_method: true
rgb_obs: true

wm:
  _target_: diwa.wm.encoder.visionwm.VisionWMObsEncoder
  ckpt_path: ${oc.env:DIWA_LOG_DIR}/wm/ckpts/vision.ckpt
  device: ${device}
  stats_path: ${oc.env:DIWA_DATA_DIR}/statistics_minmax.yaml

env:
  n_envs: 1 # For evaluation, we only need one environment (do not change this)
  n_render: 1 # or -1 
  name: ${env_name}
  env_type: ${env_type}
  best_reward_threshold_for_success: 1
  max_episode_steps: 72
  save_video: true
  load_scene_from_dataset: null
  eval_scene_from_dataset: ${oc.env:DIWA_DATA_DIR}/expert/calvin-state-50/${env_name}/val_robot_obs_scene_obs_starts_1.npz
  # The following multistep parameters are important so that we can estimate 
  # a more accurate WM state from the history of observations & actions
  n_obs_steps: ${horizon_steps}
  n_action_steps: ${horizon_steps}

model:
  _target_: diwa.model.diffusion.diffusion_eval.DiffusionEval
  ft_denoising_steps: ${ft_denoising_steps}
  predict_epsilon: True
  denoised_clip_value: 1.0
  randn_clip_value: 3.0
  #
  network_path: ${base_policy_path}
  network:
    _target_: diwa.model.diffusion.mlp_diffusion.DiffusionMLP
    time_dim: 16
    mlp_dims: [512, 512, 512]
    residual_style: True
    cond_dim: ${eval:'${obs_dim} * ${cond_steps}'}
    horizon_steps: ${horizon_steps}
    action_dim: ${action_dim}
  horizon_steps: ${horizon_steps}
  obs_dim: ${obs_dim}
  action_dim: ${action_dim}
  denoising_steps: ${denoising_steps}
  device: ${device}